# CLAUDE.md — docs/

Scope: project documentation. [`BUSINESS-DOCUMENT.md`](BUSINESS-DOCUMENT.md) is the business/domain reference (Vietnamese) — read it for product/domain questions before re-deriving them from code.

Per `AGENTS.md`'s "Project purpose": UI text, validation messages, and docs are in Vietnamese — keep new user-facing strings and documentation in this directory in Vietnamese for consistency with the rest of the project.

Execution plans (dated, per-task rationale/decisions/tradeoffs) live in `plans/` at the repo root, not here — don't conflate the two. `docs/` is for durable reference material; `plans/` is a dated work log.
